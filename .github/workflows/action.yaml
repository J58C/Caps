name: KiCad Project Automation

on:
  push:
    branches:
      - '1702'
    paths:
      - 'Caps32Project.kicad_pcb'
      - 'Caps32Project.kicad_prl'
      - 'Caps32Project.kicad_pro'
      - 'Caps32Project.kicad_sch'

jobs:
  Check-Files:
    name: Check Files
    runs-on: ubuntu-latest
    outputs:
      pcb_change: ${{ steps.check_files.outputs.pcb_change }}
    steps:
      - name: Checkout repository (branch 1702)
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          ref: 1702

      - name: Check if kicad_pcb was modified
        id: check_files
        run: |
          CHANGED_FILES=$(git diff --name-only HEAD^ HEAD)
          echo "Changed files: $CHANGED_FILES"
          if echo "$CHANGED_FILES" | grep -q "^Caps32Project.kicad_pcb$"; then
            echo "pcb_change=true" >> $GITHUB_ENV
            echo "pcb_change=true" >> $GITHUB_OUTPUT
          else
            echo "pcb_change=false" >> $GITHUB_ENV
            echo "pcb_change=false" >> $GITHUB_OUTPUT
          fi

  Compressing:
    name: Compress Files
    runs-on: ubuntu-latest
    needs: [Check-Files]
    steps:
      - name: Checkout repository (branch 1702)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: 1702

      - name: Get current date and time
        id: datetime
        run: echo "timestamp=1702-$(date +'%d%m%y%H%M')" >> $GITHUB_ENV

      - name: Create ZIP archive (outside Archive folder)
        run: zip "Caps32Project-${{ env.timestamp }}.zip" Caps32Project.kicad_pcb Caps32Project.kicad_prl Caps32Project.kicad_pro Caps32Project.kicad_sch

      - name: Checkout main branch
        run: |
          git fetch origin main
          git checkout main
          git pull origin main

      - name: Remove old ZIP file
        run: |
          rm -f Archive/Caps32Project-1702-*.zip || true

      - name: Move ZIP file to main branch
        run: |
          mkdir -p Archive
          mv "Caps32Project-${{ env.timestamp }}.zip" Archive/

      - name: Commit and Push changes to main branch
        run: |
          git add Archive/
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update archive: Caps32Project-${{ env.timestamp }} from branch 1702"
          git push origin main

  Render-Images:
    name: PCB Render
    runs-on: ubuntu-latest
    needs: [Check-Files]
    if: needs.Check-Files.outputs.pcb_change == 'true'
    steps:
      - name: Checkout branch 1702
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: "1702"

      - name: Render PCB Images
        uses: linalinn/kicad-render@main
        with:
          pcb_file: Caps32Project.kicad_pcb
          output_path: ${{ github.workspace }}/images
          
      - name: Render PCB GIF
        uses: linalinn/kicad-render@main
        with:
          pcb_file: Caps32Project.kicad_pcb
          output_path: ${{ github.workspace }}/images
          animation: gif

      - name: Commit and Push Rendered Images
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add images
          git commit -m "Update PCB render images"
          git push origin 1702

  Move-Files:
    name: Move to Main
    runs-on: ubuntu-latest
    needs: [Render-Images]
    if: needs.Check-Files.outputs.pcb_change == 'true'
    steps:
      - name: Checkout branch 1702
        uses: actions/checkout@v4
        with:
          ref: "1702"

      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: "main"
          path: main-branch

      - name: Move images to main branch
        run: |
          mkdir -p main-branch/Images/1702
          mv images/bottom.png main-branch/Images/1702/ 2>/dev/null || true
          mv images/rotating.gif main-branch/Images/1702/ 2>/dev/null || true
          mv images/top.png main-branch/Images/1702/ 2>/dev/null || true

      - name: Commit and push changes to main
        run: |
          cd main-branch
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Images/1702
          git commit -m "Update images: Rendered files 1702"
          git push origin main

      - name: Delete images folder from branch 1702
        run: |
          git checkout 1702
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -rf images
          rm -rf main-branch
          git add .
          git commit -m "Remove images folder after moving to main" || true
          git push origin 1702