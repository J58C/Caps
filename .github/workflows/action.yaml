name: KiCad Project Automation

on:
  push:
    branches:
      - '702'
    paths:
      - 'S3-1.kicad_pcb'
      - 'S3-1.kicad_prl'
      - 'S3-1.kicad_pro'
      - 'S3-1.kicad_sch'

jobs:
  Check-Files:
    name: Check Files
    runs-on: ubuntu-latest
    outputs:
      pcb_change: ${{ steps.check_files.outputs.pcb_change }}
    steps:
      - name: Checkout repository (branch 702)
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          ref: 702

      - name: Check if kicad_pcb was modified
        id: check_files
        run: |
          CHANGED_FILES=$(git diff --name-only HEAD^ HEAD)
          echo "Changed files: $CHANGED_FILES"
          if echo "$CHANGED_FILES" | grep -q "^S3-1.kicad_pcb$"; then
            echo "pcb_change=true" >> $GITHUB_ENV
            echo "pcb_change=true" >> $GITHUB_OUTPUT
          else
            echo "pcb_change=false" >> $GITHUB_ENV
            echo "pcb_change=false" >> $GITHUB_OUTPUT
          fi

  Compressing:
    name: Compress Files
    runs-on: ubuntu-latest
    needs: [Check-Files]
    steps:
      - name: Checkout repository (branch 702)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: 702

      - name: Get current date and time
        id: datetime
        run: echo "timestamp=702-$(date +'%d%m%y%H%M')" >> $GITHUB_ENV

      - name: Create ZIP archive (outside Archive folder)
        run: zip "S3-1-${{ env.timestamp }}.zip" S3-1.kicad_pcb S3-1.kicad_prl S3-1.kicad_pro S3-1.kicad_sch

      - name: Checkout main branch
        run: |
          git fetch origin main
          git checkout main
          git pull origin main

      - name: Remove old ZIP file
        run: |
          rm -f Archive/S3-1-702-*.zip || true

      - name: Move ZIP file to main branch
        run: |
          mkdir -p Archive
          mv "S3-1-${{ env.timestamp }}.zip" Archive/

      - name: Commit and Push changes to main branch
        run: |
          git add Archive/
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update archive: S3-1-${{ env.timestamp }} from branch 702"
          git push origin main

  Render-Images:
    name: PCB Render
    runs-on: ubuntu-latest
    needs: [Check-Files]
    if: needs.Check-Files.outputs.pcb_change == 'true'
    steps:
      - name: Checkout branch 702
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: "702"

      - name: Render PCB Images
        uses: linalinn/kicad-render@main
        with:
          pcb_file: S3-1.kicad_pcb
          output_path: ${{ github.workspace }}/images
          
      - name: Render PCB GIF
        uses: linalinn/kicad-render@main
        with:
          pcb_file: S3-1.kicad_pcb
          output_path: ${{ github.workspace }}/images
          animation: gif

      - name: Commit and Push Rendered Images
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add images
          git commit -m "Update PCB render images"
          git push origin 702

  Move-Files:
    name: Move to Main
    runs-on: ubuntu-latest
    needs: [Render-Images]
    if: needs.Check-Files.outputs.pcb_change == 'true'
    steps:
      - name: Checkout branch 702
        uses: actions/checkout@v4
        with:
          ref: "702"

      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: "main"
          path: main-branch

      - name: Move images to main branch
        run: |
          mkdir -p main-branch/Images/702
          mv images/bottom.png main-branch/Images/702/ 2>/dev/null || true
          mv images/rotating.gif main-branch/Images/702/ 2>/dev/null || true
          mv images/top.png main-branch/Images/702/ 2>/dev/null || true

      - name: Commit and push changes to main
        run: |
          cd main-branch
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Images/702
          git commit -m "Update images: Rendered files 702"
          git push origin main

      - name: Delete images folder from branch 702
        run: |
          git checkout 702
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -rf images
          rm -rf main-branch
          git add .
          git commit -m "Remove images folder after moving to main" || true
          git push origin 702